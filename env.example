# SENTRY_API_URL is optional — defaults to the production Sentry API if not set
# SENTRY_API_URL=https://custom-url-if-needed.example.com
AGENT_NAME=your_agent_name
CLAWKEY_API_BASE=https://api.clawkey.ai/v1
CLAWKEY_IDENTITY_PATH=~/.openclaw/identity/device.json
